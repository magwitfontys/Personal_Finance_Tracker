services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: personal-finance-backend:dev
    container_name: pft-backend
    ports:
      - "8081:8081"
    volumes:
      - backend-h2:/app/data
    restart: unless-stopped

volumes:
  backend-h2:
