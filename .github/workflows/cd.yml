name: CD (Docker on self-hosted, branch CI/CD)

on:
  push:
    branches: ["CI/CD"]      # only auto-run on the CI/CD branch
  workflow_dispatch:          # also show the "Run workflow" button

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ping:
    name: Runner ping
    runs-on: [self-hosted, Windows, X64]   # match your runner's labels
    steps:
      - name: Hello from runner
        shell: powershell
        run: Write-Host "Runner OK on $env:COMPUTERNAME"

  deploy:
    name: Docker build & run locally
    needs: ping
    runs-on: [self-hosted, Windows, X64]
    steps:
      - uses: actions/checkout@v4

      - name: Docker is available
        run: |
          docker --version
          docker info

      - name: Build image (multi-stage Dockerfile)
        run: docker build -t cddemo:latest back-end

      - name: Stop/remove existing container (if any)
        shell: powershell
        run: |
          if (docker ps -a -q -f "name=cddemo") {
            docker stop cddemo | Out-Null
            docker rm cddemo   | Out-Null
          }

      - name: Run container
        run: docker run -d --name cddemo -p 8080:8080 cddemo:latest

      - name: Show status
        run: docker ps --filter "name=cddemo"
